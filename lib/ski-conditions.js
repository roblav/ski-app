//Require the DB

var skiData = require(../database/20160302);

// My Database is a list of JSON files
// I need to figure out how I can grab the file name from the URL
// then display that data for that record

